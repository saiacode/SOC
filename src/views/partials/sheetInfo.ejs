<h2>Información de la Hoja de Cálculo</h2>
<div class="sheet-info">
    <div class="sheet-item"><strong>Título de la hoja:</strong> <%= sheetInfo.title %></div>
    <div class="sheet-item"><strong>ID de la hoja:</strong> <%= sheetInfo.sheetId %></div>
    <div class="sheet-item"><strong>Número de filas:</strong> <%= sheetInfo.rowCount %></div>
    <div class="sheet-item"><strong>Número de columnas:</strong> <%= sheetInfo.columnCount %></div>
</div>